[project]
name = "warehouse"
version = "0.1.0"
description = "dbt project restating the base and order-book identities over actuals"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.8",
    "dbt-duckdb>=1.8",
]

[tool.uv]
package = false
